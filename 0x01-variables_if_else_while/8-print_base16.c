#include <stdio.h>
/**
 * main - Entry Point
 * Return: Always 0 (Success)
 */

int main(void)
{
	char t;

	int d;

	t = 'a';
	d = 0;
	while
		(d < 10) {
			putchar(d + '0');
			d++;
		}
	while
		(t <= 'f') {
			putchar(t);
			t++;
		}
	putchar('\n');
	return (0);
}

