#include <stdio.h>
/**
 * main - Entry point
 * Return: Always 0 (Success)
 */

int main(void)
{
	char s;

	char t;

	s = 'a';
	t = 'A';
	while
		(s <= 'z') {
			putchar(s);
			s++;
		}
	while
		(t <= 'z') {
			putchar(s);
			s++;
		}
	putchar('\n');
	return (0);
}

