#include "main.h"
#include<string.h>
/**
 * _strcmp - code compares two strings.
 * @s1:input value string1 to compare.
 * @s2: input value string two to compare.
 * Return: 0.
 */

int _strcmp(char *s1, char *s2)
{
	int result;

	result = strcmp(s1, s2);
	return (result);
}
