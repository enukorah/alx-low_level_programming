#include "main.h"
/**
 *  a function that prints the sign of a number.
 */
int print_sign(int n);
{
	if (n > 0)
	{
		_putchar(43);
		return (1);
	}
	else if (n < 0)
	{
		_printchar(45);
		return (-1);
	}
	else 
	{
		_printchar(48)
		return (0)
	}
	_printchar('\n');
}
